package menus;

import main.Texture;
import main.TexturedGameObject;

public class VolumeScale extends TexturedGameObject {
    
    public VolumeScale(float x, float y, float w, float h, Texture tex, String id) {
        super(x, y, 0, 0, 0, 0, w, h, id, tex);
    }
    
}
